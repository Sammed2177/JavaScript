const ages = [12, 22, 12, 34, 45, 55];
console.log(ages);

// filter method
const result =  ages.filter(age => age > 18 );
console.log(result);

// map method
const a = ages.map(age => age + 18);
console.log(a);

// reduce method
